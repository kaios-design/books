books = books

no-books = Move txt files to "books" folder to start reading.

## softkey
open = open
read = read
options = options
select = select
cancel = cancel
ok = OK

## Dialog
goto-page = goto page
